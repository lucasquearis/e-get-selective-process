{
  "name": "back-end",
  "version": "1.0.0",
  "description": "Fake server with fake database",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json -p 8080",
    "docker-image": "docker build -t e-get-back-end .",
    "docker-container": "docker run -d --rm -p 8080:8080 --name e-get-back-end e-get-back-end"
  },
  "author": "Lucas A. Santos",
  "license": "ISC",
  "dependencies": {
    "json-server": "0.17.2"
  }
}