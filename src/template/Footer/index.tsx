import styled from "styled-components";

const HeaderContent = styled.header`
  height: 10vh;
  max-width: 1440px;
  margin: 0 auto;
`;

function Footer() {
  return <HeaderContent>Footer</HeaderContent>;
}

export default Footer;
